services:
  eshopdb:
    image: postgres

  seq:
    image: datalust/seq:latest

  distributedcache:
    image: redis

  redisinsight:
    container_name: redisinsight

  messagebus:
    image: rabbitmq:management

volumes:
  postgres_eshopdb:
  redisinsight_data:  